# Node modules
node_modules/

# Environment variables (all variants)
.env
.env.local
.env.*.local
*.env
*.env.local

# Firebase service account keys (both root and backend)
serviceAccountKey.json
backend/*serviceAccountKey.json
*serviceAccountKey.json

# Build outputs (optional)
dist/
build/